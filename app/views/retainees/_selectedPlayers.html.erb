<%= content_tag :span,
  link_to_remote(image_tag('cancel.png', :border => 0, :size => "16x16") +
    "&nbsp;#{selectedPlayers.player} (#{selectedPlayers.pos})",
  :url => { :action => "remove_player_from_retainee_list" },
  :with   => "'id=' + #{selectedPlayers.id}"),
  :id => "selectedPlayer_#{selectedPlayers.id}",
  :class => "player"
%><br />
<% javascript_tag do -%>
  Effect.Fade('<%= "player_#{selectedPlayers.id}" %>')
<% end -%>